---
"@pulseguard/api": patch
"@pulseguard/auth": patch
"@pulseguard/db": patch
"@pulseguard/env": patch
---

Initialize published package metadata: set package versions, fix `exports` maps (added `./routers` subpath to `@pulseguard/api`), and normalize `bun.lock` workspace entries.
