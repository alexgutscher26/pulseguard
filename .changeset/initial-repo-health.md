---
"@steadystack/api": patch
"@steadystack/auth": patch
"@steadystack/db": patch
"@steadystack/env": patch
---

Initialize published package metadata: set package versions, fix `exports` maps (added `./routers` subpath to `@steadystack/api`), and normalize `bun.lock` workspace entries.
