{
  "name": "@pulseguard/probe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "tsc",
    "start": "bun src/index.ts",
    "docker:build": "docker build -t pulseguard-probe ."
  },
  "dependencies": {
    "@pulseguard/core": "workspace:*",
    "@pulseguard/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
