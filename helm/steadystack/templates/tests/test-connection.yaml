{{- if .Values.web.enabled }}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "steadystack.fullname" . }}-test-connection"
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "steadystack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: curl
      image: curlimages/curl:latest
      command:
        - curl
        - -sf
        - --retry
        - "5"
        - --retry-connrefused
        - http://{{ include "steadystack.fullname" . }}-web:{{ .Values.web.service.port }}/api/health
  restartPolicy: Never
{{- end }}
